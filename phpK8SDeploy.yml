apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    name: phpapp
  name: php-controller
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: phpapp
    spec:
      containers:
      - image: ananthkannan/myspringbootapp:latest
        name: phpapp
        ports: 
        - name: phpapp
          containerPort: 80
---
# Node Port Service
apiVersion: v1
kind: Service
metadata:
  labels:
    name: phpapp
  name: phpapp
spec:
  type: NodePort
  ports:
    - port: 8085
      targetPort: 8085
  selector:
    name: phpapp
---